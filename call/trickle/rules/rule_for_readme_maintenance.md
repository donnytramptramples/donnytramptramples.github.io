When updating the project:
- Check if changes affect the project structure, features, or usage
- Update README.md in trickle/notes/ if necessary
- Keep documentation synchronized with actual implementation
- Update feature list when new functionality is added
- Update project structure section when files are added/removed
- Update technology stack if new libraries are integrated